# Copy this file to .env and replace the placeholder with your actual key.
# .env is listed in .gitignore — never commit your real key.
#
# Get a free Groq API key (no credit card required) at:
# https://console.groq.com

GROQ_API_KEY=your_key_here

# For collect_places_reviews.py — official Google Places API (New).
# Create a project at https://console.cloud.google.com/, enable "Places API (New)",
# then create an API key under APIs & Services -> Credentials.
GOOGLE_MAPS_API_KEY=your_key_here
